/***
 *--------------------------I.DOM (standard structure of w3s)
 * document
 * element
 * attribute
 *
 *
 * -------------------------II.JS (giup truy xuat vao thanh phan trong DOM)
 * run on: Browser | server (NODEJS)
 * with Browser: HTML -> create DOM -> browser want access DOM through "DOM API"
 * "DOM API is WEB API in this situation" is wrote by js, ...
 *
 *
 * ------------------------------------III.Kien thuc cot loi
 * 1. JSON
 * 2. Promise: -> (Promise - PromiseExample)
 * 3. Fetch (backend -> api(url - cong giao tiep) -> fetch -> json -> json.parse -> get data -> render data
 * use fetch API)
 * 	3.1 JSON SERVER: lib to fake API -> call is REST API (create fake api and test it)
 *	3.2 Postman: dung de test CRUD khi front end chua lam xong de thuc hien,
		use Postman to Post,
		Rest Standard: CRUD
		C -> Post : (switch to body tab -> create without id property, that is created automatic)
		R -> Get
		U -> Put/Patch ( switch to body tab -> edit -> add id want edit ".../id" - >send)
		D -> Delete (/id)
 * DOM location
 * Local storage
 * Session storage
 * coding convention
 */